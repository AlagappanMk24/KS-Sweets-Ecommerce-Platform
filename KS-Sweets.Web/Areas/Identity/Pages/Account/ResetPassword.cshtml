@* @page
@model ResetPasswordModel
@{
    ViewData["Title"] = "Reset password";
}

<h1>@ViewData["Title"]</h1>
<h2>Reset your password.</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <input asp-for="Input.Code" type="hidden" />
            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                <label asp-for="Input.Email" class="form-label"></label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="form-floating mb-3">
                <input asp-for="Input.Password" class="form-control" autocomplete="new-password" aria-required="true" placeholder="Please enter your password." />
                <label asp-for="Input.Password" class="form-label"></label>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="form-floating mb-3">
                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" aria-required="true" placeholder="Please confirm your password." />
                <label asp-for="Input.ConfirmPassword" class="form-label"></label>
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">Reset</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} *@

@page
@model ResetPasswordModel
@{
    ViewData["Title"] = "Reset Your Password";
}

<style>
    /* 🎨 COLOR CONSTANTS - COPIED FOR CONSISTENCY */
    :root {
        --color-primary-dark: #6B4226;
        --color-secondary-light: #F8BBD0;
        --color-accent-dark: #C79E89;
        --color-bg-light: #FFF8F0;
        --color-accent-text: #E57373;
    }

    body {
        background-color: var(--color-bg-light);
        overflow-x: hidden;
    }

    .auth-card {
        border-radius: 15px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        border: none;
        overflow: hidden;
    }

    .auth-card-header {
        background-color: var(--color-primary-dark) !important;
        color: white;
        padding: 25px 0;
        border-bottom: 7px solid var(--color-secondary-light);
    }

    .auth-card-body {
        padding: 40px !important;
    }

    /* ------------------------ BUTTONS AND EFFECTS ------------------------ */

    .btn-auth-primary {
        background-color: var(--color-primary-dark);
        border-color: var(--color-primary-dark);
        color: white;
        font-weight: 700;
        border-radius: 10px;
        padding: 12px 0;
        font-size: 1.15rem;
        transition: all 0.3s ease;
    }

        .btn-auth-primary:hover {
            background-color: #5A351D;
            border-color: #5A351D;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }

    .auth-links a {
        color: var(--color-primary-dark);
        font-weight: 500;
        text-decoration: none;
        transition: color 0.2s;
    }

        .auth-links a:hover {
            color: var(--color-accent-text);
            text-decoration: underline;
        }

    .form-control:focus {
        border-color: var(--color-secondary-light) !important;
        box-shadow: 0 0 0 0.25rem rgba(248, 187, 208, 0.6) !important;
    }
</style>


<div class="row justify-content-center mt-5">
    <div class="col-12 col-sm-10 col-md-8 col-lg-7 col-xl-6">
        <div class="card auth-card">

            <div class="card-header auth-card-header text-center">
                <h2 class="py-1 mb-0 display-6 fw-bold">
                    <i class="bi bi-shield-lock-fill me-2"></i> @ViewData["Title"]
                </h2>
            </div>

            <div class="card-body auth-card-body">
                <div class="row">
                    <div class="col-12">
                        <h4 class="text-secondary text-center mb-5 fw-light">
                            Enter your email and new secure password.
                        </h4>

                        <form method="post">
                            <div asp-validation-summary="ModelOnly" class="text-danger mb-4 text-center" role="alert"></div>

                            @* Hidden field for the reset token *@
                            <input asp-for="Input.Code" type="hidden" />

                            @* Email Input *@
                            <div class="form-floating mb-4">
                                <input asp-for="Input.Email" class="form-control" autocomplete="email" aria-required="true" placeholder="name@example.com" />
                                <label asp-for="Input.Email" class="form-label text-muted">Email Address</label>
                                <span asp-validation-for="Input.Email" class="text-danger"></span>
                            </div>

                            @* New Password Input *@
                            <div class="form-floating mb-4">
                                <input asp-for="Input.Password" class="form-control" autocomplete="new-password" aria-required="true" placeholder="New Password" />
                                <label asp-for="Input.Password" class="form-label text-muted">New Password</label>
                                <span asp-validation-for="Input.Password" class="text-danger"></span>
                            </div>

                            @* Confirm Password Input *@
                            <div class="form-floating mb-5">
                                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" aria-required="true" placeholder="Confirm Password" />
                                <label asp-for="Input.ConfirmPassword" class="form-label text-muted">Confirm New Password</label>
                                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                            </div>

                            <div class="mb-4">
                                <button type="submit" class="w-100 btn btn-auth-primary">
                                    <i class="bi bi-lock-fill me-1"></i> Reset Password
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}